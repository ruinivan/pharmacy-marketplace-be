package pharmacymarketplace.order.enums;

public enum OrderStatusEnum {
    PENDING,
    AWAITING_PAYMENT,
    AWAITING_PRESCRIPTION,
    PROCESSING,
    SHIPPED,
    DELIVERED,
    CANCELLED,
    REFUNDED
}
